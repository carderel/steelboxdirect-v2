# Trailing slash variants of the old flat city URLs. Google indexed the
# slash forms and the adapter generated rules in astro.config.mjs only
# cover the no slash forms, so those slash URLs were returning 404.
/cincinnati-shipping-containers/ /locations/ohio/cincinnati-shipping-containers/ 301
/dayton-shipping-containers/ /locations/ohio/dayton-shipping-containers/ 301
/indianapolis-shipping-containers/ /locations/indiana/indianapolis-shipping-containers/ 301
/louisville-shipping-containers/ /locations/kentucky/louisville-shipping-containers/ 301

# Leftover Partytown asset paths from the removed integration. Google
# flagged them, so send everything under that prefix home with a 301.
/~partytown / 301
/~partytown/* / 301

# ---- BEGIN generated: retired county permit URLs (2026-09-16, rewritten 2026-09-17) ----
# The 77 pages at /permits/{state}/{county}/ were folded into the 11 state
# pages on 2026-09-16, each one keeping the slug its URL ended in as a section
# anchor. Those 77 URLs were first restated one line each, in both slash forms,
# 154 rules across this file and astro.config.mjs, and that was the defect:
# Cloudflare Pages honours only the first 110 or so lines of _redirects and
# drops the rest in silence, with no build error, no deploy warning and no log
# line. Measured against production on 2026-09-17, not theorised: all 77
# trailing slash forms 301d, 21 of the 77 bare forms 301d, the other 56
# returned 404, and the break tracked POSITION IN THE FILE rather than anything
# about the rule. Rule 105 worked and rule 109 did not.
#
# So the 154 lines are now 22, one PLACEHOLDER rule per state per slash form,
# and the whole file is 34 rules. :county is a Cloudflare Pages placeholder: it
# matches exactly one path segment and is substituted into the destination,
# fragment included. Verified by serving a fixture through wrangler pages dev,
# the same asset runtime Pages itself runs, rather than assumed from the docs:
#   /permits/ohio/hamilton-county/   301 -> /permits/ohio/#hamilton-county
#   /permits/ohio/hamilton-county    301 -> /permits/ohio/#hamilton-county
#   /permits/ohio/hamilton-county/x  no match, a placeholder spans no slash
#
# ONE RULE PER STATE, not the two lines /permits/:state/:county would have
# taken, so a made up state slug cannot 301 into a page that does not exist. A
# made up county under a REAL state does still 301 to that state page, and that
# is accepted rather than patched: nothing links or indexes such a URL so
# nothing crawls it, and there is no other real subpath under /permits/{state}/
# for a placeholder to swallow. Adding one means retiring the placeholder.
#
# BOTH SLASH FORMS LIVE HERE. astro.config.mjs used to carry the bare forms as
# an adapter generated block, because Astro normalises a trailing slash off any
# redirect key and could not state the slash form at all. A placeholder rule
# states both forms in two lines, so that block is gone and the whole rule set
# is in one file, in one syntax, countable in one place.
#
# GENERATED, NOT TYPED, from src/data/permitCounties.ts:
#   permitStates.flatMap((s) => [
#     `${s.path}:county/ ${s.path}#:county 301`,
#     `${s.path}:county ${s.path}#:county 301`,
#   ])
# src/lib/compliance/permit-county-guard.test.ts recomputes this block and
# fails if a state is missing or one is named that no longer exists, and it
# fails if dist/_redirects ever carries 100 rules or more. Do not hand edit a
# line.
/permits/georgia/:county/ /permits/georgia/#:county 301
/permits/georgia/:county /permits/georgia/#:county 301
/permits/indiana/:county/ /permits/indiana/#:county 301
/permits/indiana/:county /permits/indiana/#:county 301
/permits/kentucky/:county/ /permits/kentucky/#:county 301
/permits/kentucky/:county /permits/kentucky/#:county 301
/permits/michigan/:county/ /permits/michigan/#:county 301
/permits/michigan/:county /permits/michigan/#:county 301
/permits/missouri/:county/ /permits/missouri/#:county 301
/permits/missouri/:county /permits/missouri/#:county 301
/permits/new-york/:county/ /permits/new-york/#:county 301
/permits/new-york/:county /permits/new-york/#:county 301
/permits/ohio/:county/ /permits/ohio/#:county 301
/permits/ohio/:county /permits/ohio/#:county 301
/permits/south-carolina/:county/ /permits/south-carolina/#:county 301
/permits/south-carolina/:county /permits/south-carolina/#:county 301
/permits/texas/:county/ /permits/texas/#:county 301
/permits/texas/:county /permits/texas/#:county 301
/permits/virginia/:county/ /permits/virginia/#:county 301
/permits/virginia/:county /permits/virginia/#:county 301
/permits/west-virginia/:county/ /permits/west-virginia/#:county 301
/permits/west-virginia/:county /permits/west-virginia/#:county 301
# ---- END generated: retired county permit URLs ----
