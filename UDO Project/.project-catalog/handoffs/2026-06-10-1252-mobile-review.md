# Handoff Packet — Mobile Experience Review (interactive)

**Timestamp:** 2026-06-10 12:52 EDT
**Reason for handoff:** Owner restarting Claude Code to load the newly-installed Playwright MCP, then resuming with **interactive** mobile testing.
**Repo state:** `main` clean & pushed, HEAD `a209ba4`. Everything deployed to Cloudflare.

---

## First actions next session
1. Confirm the Playwright MCP loaded: `/mcp` should list **playwright** (added at user scope 2026-06-10; chromium installed). If missing, `claude mcp list`.
2. Start the dev server: `npm run dev` → http://localhost:4321/ (runs on 4321).
3. Drive **interactive** mobile testing with the Playwright MCP (device emulation + tap/scroll/submit), not just screenshots.

## What to test (key templates) — primary width iPhone ~390px, spot-check tablet ~768px
- **Home** `/`
- **Product hub** `/shipping-containers-for-sale/` + a **product detail** `/shipping-containers-for-sale/20-foot-shipping-container/`
- **City page** `/cincinnati-shipping-containers/`
- **Condition guide** `/condition/`  (reframed to Wind & Water Tight only)
- **Quote form** `/quote/`
- **Use-case page** `/for/farmers/`
- **Locations hub** `/locations/`

## Interactive checks that static screenshots miss (this is the point of using the MCP)
- Open/close the **mobile hamburger nav**; check the **Locations dropdown** and the secondary **"Containers Use Type"** strip behave on touch.
- **Tap-target sizes** (nav links, CTA buttons, footer social icons, the Carder Creative credit link).
- The **Call Now** `tel:` link.
- **Quote form on mobile**: field layout, the condition dropdown (now just "Wind & Water Tight (Used)" + "Not sure"), keyboard/scroll behavior, inline validation. ⚠️ A full submit fires a real email to **carder.creative@gmail.com** and writes a Supabase lead row — if you submit, mark it an obvious test (e.g. name "TEST mobile QA").
- **Horizontal overflow / sideways scroll** on any page; font legibility; the **`/size/calculator/`** page (React-only — verify it renders/usable on mobile).
- **Footer** at mobile width (2-col collapse, the Doug Froh social block, the bottom credit bar).

## Known suspects (from one headless homepage shot taken 2026-06-10 — verify first)
- An orange **"Delivery Inclu…"** hero button looked possibly **truncated** at 390px.
- The **secondary nav strip** looked **tight/cramped** on mobile.

## Constraints (carry forward)
- **No prices / dollar amounts** anywhere (HARD STOP).
- **Condition = Wind & Water Tight (used) ONLY** — edit copy via `src/data/condition.ts`; never claim shipping certification; don't reintroduce Cargo Worthy / One-Trip.
- Stats live in `src/data/stats.ts`; FAQ on the product hub renders from one `faqs[]` array (schema + visible) — keep that pattern.

## Fix → ship loop
Make fixes → `npm run build` (must be clean) → commit (own commits) → `git push origin main` (auto-deploys to Cloudflare).

## Safety net
- Restore tag `backup-pre-refactor-2026-06-10` (pre-refactor) and snapshot `~/Documents/Backups/Container-Site_2026-06-10_pre-refactor/`. Current HEAD is *post*-refactor and is the good state.

## Tooling fallback (if MCP misbehaves)
Headless screenshot: `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=390,3200 --screenshot=/tmp/out.png http://localhost:4321/<path>`
