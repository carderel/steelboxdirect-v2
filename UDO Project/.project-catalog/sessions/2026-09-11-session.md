# Session log: 2026-09-11

- **Session opened on "resume".** Live at 34b5b90, `main == origin`, clean sync. No commits yet today.
- **Carried in:** T-215 (77 county permit pages + 11 state indexes) live and IndexNow-submitted,
  sitemap 150 URLs. T-209 CI proof closed by 719a04a.
- **T-214 IN FLIGHT, the approved next step:** build the missing `/locations/{state}/` layer, 11 state
  pages derived from `cities.ts`. That URL segment 404s today. Honest home-vs-depot framing per city,
  links to `/permits/{state}/` and `/cost/`, NO prices (hard stop). Dispatched to astro-developer.
- **T-214 ride-along IN FLIGHT:** render `areaProfile` + `geography` into the city-page HTML. Verified
  absent from HTML, present in the `.md` twin. Also folds in the stale T-212 comment at
  `src/pages/locations/[state]/[citySlug].astro:92-98`. Dispatched to astro-developer.
- Both subagents told: no commit, no push. Verifier gate still to run before any commit.
- **Open owner decisions, unchanged:** T-208 Supabase A or B (leads reach Doug by email only);
  send smallbarndo; 10ft SKU question. T-203 housedigest follow-up due 2026-09-17.
- **T-214 ride-along DONE, in tree, not committed.** areaProfile + geography now render in city-page
  HTML, mirroring the `.md` twin's order and labels. 15 of 15 city pages carry the new section.
  Huntington's "Ohio River bends between West..." sentence now greps out of the built HTML.
  Build clean, 708 tests pass (same as baseline). No dollar amount added.
- The T-212 comment at `[citySlug].astro:93-99` was stale on three counts and is replaced. The
  `dateModified` prop is a documented fallback, not inert; the committed routeLastmod table still wins.
- **T-214 state pages BUILT, in tree, not committed.** New file
  `src/pages/locations/[state]/index.astro` emits 11 URLs: georgia, indiana, kentucky, michigan,
  missouri, new-york, ohio, south-carolina, texas, virginia, west-virginia. Build clean, 708 tests,
  zero dollar signs in the generated HTML. Home-vs-depot framing verified per city; Ohio correctly
  renders mixed (Cleveland flagged Depot, the other three Home).
- **Gap found and dispatched:** nothing linked to the new pages but the sitemap. Agent C is adding
  the state tier to `/locations/` and the linked state crumb to city breadcrumbs.
- **DEBT after commit:** `npm run generate:route-lastmod`, commit the diff (else the freshness guard
  goes red), then `npm run indexnow`.
- **Hub + crumb DONE, in tree, not committed.** `/locations/` gets a "Browse by state" band linking
  all 11 state pages; the two existing grids are untouched on purpose, because they group by
  fulfillment model (home vs depot) and that is the page's honesty mechanism. State is a crosscutting
  axis, so it gets its own tier rather than regrouping Ohio under one false label.
- City breadcrumbs now read Home > Locations > {State} > {City}, state crumb linked, JSON-LD moved
  with it. The stale "no state-level crumb" comment is replaced.
- **Verifier gate running** on all three agents' work before any commit.
- **VERIFIER GATE: PASS on all nine criteria (A-I).** Fresh build, 708/708 tests, pricing hard stop
  clean, HS-003 depot disclosure present on all seven depot states, Ohio labelled per city, permits
  defer to the buyer. areaProfile prose diffed byte-for-byte against the `.md` twin: 15 checked,
  0 diffs, 1042 words, matching the audit figure. Hub diff is 85 insertions and zero deletions.
- The verifier confirmed the HS-003 guard actually scans the new file and that neither the guard nor
  its allowlist was modified, so nothing was whitelisted to pass.
- **One cosmetic finding, fix in flight:** the state page renders "a Ohio city" / "a Indiana city".
  Article is hardcoded. Being derived instead. Commit waits for it.
- **Article fixed** with a derived helper, not a name list. All 11 pages read correctly. Known and
  accepted limit: it tests first letter, not sound, so a future U-state would read "an Utah city".
  No U-state exists in `cities.ts`; the caveat lives in the comment.
- **T-214 COMMITTED, NOT PUSHED.** `4476c54` (11 state pages + areaProfile in HTML + state crumb +
  hub state tier, 3 files, 461 insertions) and `5016e97` (route lastmod, 3 entries moved).
  708 tests green after regeneration. `main` is 2 ahead of `origin/main`.
- **Waiting on owner:** go for the push. After push, run `npm run indexnow` for the 11 new URLs.
- **3D container viewer spike opened (owner request).** Source:
  `~/.gemini/antigravity/scratch/shipping-container-3d`. Testing on the 20ft product page only.
  Three defects found in its EMBED_GUIDE.md before touching anything: the file inventory omits
  `containerModel.js` and `textures.js` (the embed is broken without them); the "standalone,
  zero external dependencies" files still fetch Three.js from esm.sh; and either way the widget puts
  a third-party runtime dependency on a commercial page. The exploded JPGs are 684-804 KB each.
  Placed below the hero, NOT in `.listing-media`, because that image is the LCP element.
- **3D spike WIRED AND RENDERING on the 20ft page.** Six files in `public/3d/`, `[slug].astro` +72
  lines gated to the 20ft slug. Build clean, 708 tests. Absent from both 40ft pages. NOT COMMITTED.
- **Biggest defect, measured:** desktop downloads the 682 KB mobile fallback JPG it never shows,
  because the fallback markup lives inside the embed and `display:none` does not stop an image load.
  Deleting that div drops desktop first-party weight from 770 KB to 88 KB.
- **Security:** the embed is same-origin, so esm.sh code can reach the parent page and the lead
  forms, and an import map cannot carry SRI. Vendor three.js, or at minimum add
  `sandbox="allow-scripts"` to the iframe.
- **Also:** `/3d/embed-20ft.html` is indexable and would compete with the product page (needs
  noindex); the render loop runs at 60fps while out of view; no keyboard rotate/zoom; loose camera
  framing; the exploded JPG shows a brown "wooden" roof, what looks like four door leaves on a 20ft,
  and garbled stencil text, on a site that publishes a markings guide.
- **Owner decision open:** polish the cheap defects then review, or drop the widget.
- **3D controls were dead. ROOT CAUSE FOUND AND FIXED.** `embed-core.js` called
  `setExplodedProgress()` and `setDoorsProgress()`, neither of which exists on ContainerModel; the
  real methods are `setExplosionFactor(t)` and `setDoorAngle(radians)`. Both bad calls sit in
  `animate()` before `renderer.render()`, so the loop threw every frame and the picture froze, which
  is why Hotspots and Reset View also looked broken. They were never broken.
- Fixed two lines in `public/3d/embed-core.js`. Verified in a real browser: explode and doors both
  animate to full, both door leaves swing, zero console errors, 708 tests pass.
- **No regression test added**: it is a static asset under `public/` that the suite does not execute.
  This is browser-verified, not test-locked.
- **Still broken upstream:** the same bug is in `~/.gemini/antigravity/scratch/shipping-container-3d`
  and in all three `standalone-*.html`. Only our copy is fixed.
- **New defect seen:** one door leaf renders as a plain white untextured panel. Not yet diagnosed.
- **The white door panel was NOT a defect. My earlier flag was wrong.** The decal planes are
  genuinely transparent (8.3% and 11.4% opaque, sampled in-browser). The white surface is the
  "Interior Face Plate" at `containerModel.js:762-768` using `materials.interiorWall` (0xd8dee4),
  the painted inside face of the door leaf. Real container door interiors are light gray. Left alone.
- Cosmetic inconsistency noted, not touched: `interiorWall` is applied at one site only, the door,
  so wall interiors still read green.
- **Owner's two defaults applied** to `public/3d/embed-core.js`: opens at 50% exploded (state,
  model, slider and button all driven from one constant) and 25% closer, scaled about the orbit
  target so framing tightens rather than drifts. Reset View returns to the new framing.
- Verified in-browser: explode 0.5 everywhere, slider "50", button active; camera distance 11.56 vs
  15.42, a ratio of 0.7497. Build clean, 708 tests, `dist/3d` carries it. Still uncommitted.
- **3D viewer now on all three product pages.** Gate generalised into a `VIEWER_MODELS` lookup keyed
  by slug (embed, still, modelLabel, isoCode). One section, not three. Head bars read
  "20ft standard 22G1", "40ft standard 42G1", "40ft high cube 45G1". No page references another
  page's model. Build clean, 708 tests. The patched engine survived the copy, confirmed by grep.
- Verified both new models in a real browser: 40std ocean blue #155289, 40hc safety orange #e25816
  at 9'6" height, both open at 50% exploded and 25% closer, doors swing, zero console errors.
- **NEW ISSUE:** at 1200px the 25% zoom CROPS both 40-footers; the box runs off the left edge. The
  zoom was tuned on the 20ft, which is half as long. Owner decision: accept the crop, or scale the
  zoom by model length.
- New bytes on disk 1,601,905, 98% of it the two new JPGs. esm.sh is now a runtime dependency on
  three commercial pages instead of one. `public/3d/` is still untracked; nothing committed.
- **The Astro dev server was killed twice for low memory.** Measured: about 62 MB genuinely free
  (`vm_stat`, 3,761 free pages), no single hog, a long tail of VS Code and Chrome helpers plus a VM.
  Not the WebGL pages; the second kill happened with the browser closed.
- **Now serving the built `dist/` with `python3 -m http.server 4321`** from inside `dist/`, far
  lighter than Vite. Same port, so existing links work. All three product pages and the embeds
  return 200. Trade-off: no hot reload, so any source edit needs `npm run build` first.
- **BLOCKED on machine memory, not on the project.** A third background server was killed, this time
  a tiny `python3 -m http.server`, which rules out process size: any background command I start gets
  killed under pressure. Measured: 61 MB free, and swap at 15,859 MB used of 17,408 MB. The machine
  is paging hard. Owner must run the preview from their own Terminal, or free memory first.
- No code touched by any of this. All 3D work and all T-214 work remain intact.
- **Memory recovered and the dev server is up again.** Free 4,964 MB against 61 MB, swap no longer
  thrashing. Running the real Vite dev server, so hot reload works. All three product pages and all
  three embeds return 200. (Wall clock rolled to 2026-09-14 during the gap.)
