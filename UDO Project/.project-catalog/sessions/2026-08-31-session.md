# Session: 2026-08-31 (same working session, continued)

Tags: #t-183-rto-99-down #brainstorming-gate #owner-desk-gate
LLM: Claude Opus 5 (claude-opus-5[1m]), Claude Code CLI
Status: OPEN. `main == origin == 9a78f7a`. No commits today. One uncommitted spec file.

Transcript: `.project-catalog/history/2026-08-31-session-transcript.md` (cycle 17 onward;
cycles 1-16 are in the 2026-08-27 and 2026-08-28 transcripts).
Earlier days of this session: `sessions/2026-08-27-session.md`, `sessions/2026-08-28-session.md`.

## The day's work: planning the $99 RTO change (T-183). NOT built.

Doug texted the owner on 2026-08-28: from **2026-09-01** (tomorrow) My Container Rental makes
**$99** the standard RTO down payment on standard containers, replacing 10% down, with **20% down**
beyond 150 miles. June's $99 special **roughly doubled sales volume**, which is why it is becoming
standard. Classified ARCHITECTURAL under the brainstorming skill.

**Spec written and sent: `docs/superpowers/specs/2026-08-31-rto-99-down-design.md`.**
**AWAITING OWNER APPROVAL. Nothing may be built until he says yes.**

### Two owner rulings that constrain the build

1. **Keep the existing application wording.** Doug's memo says "no application or special approval
   is needed". The owner called him, then ruled: the memo is the official status, but everything
   observable still requires an application, so SBD publishes what is observably true. Every
   "application" and "subject to third-party approval" string on `/rent-to-own/` stays. Listed in
   the spec as explicitly out of scope so a later pass cannot tidy it to match the memo.
2. **Wide scope, and the CTA is his design.** He rejected both of the orchestrator's city-page
   options in favour of a third: a CTA graphic in the empty right column beside the delivered-price
   block, in his own words, linking to `/rent-to-own/`.

### Two orchestrator errors, both owner-corrected, both recorded in the spec

1. **The 150 miles is from the DEPOT, not from Cincinnati.** The orchestrator assumed Cincinnati,
   computed straight-line distances for all 15 metros, concluded 8 could not offer $99, and told
   the owner his Norfolk screenshot was the failure case. The owner corrected it: "$99 is available
   to any location but if it's over 150 miles from a depot then it's 20% down." Consistent with the
   site's existing "nationwide from depot hubs" language. **The whole analysis was void.** Upside:
   the CTA is evergreen and identical on all 15 city pages, with no geo-awareness and no new
   distance data.
2. **The specialty carve-out is a near no-op.** Doug's 20%-for-modified clause applies to nothing
   SBD sells; the catalog is 20ft, 40ft, 40ft HC, and three pages already state SBD does not sell
   modified containers.

### The settled CTA copy

Headline, owner's words: "Find out how you can get your shipping container for one easy down
payment as low as $99!"

Small print, with the depot correction folded in: "Pending third-party approval. $99 applies to
standard containers delivered within 150 miles of the depot your container ships from; 20% down
beyond that. Delivery distance also affects your monthly payment."

### Still required before or during the build

- A **decision record** at `.project-catalog/decisions/2026-08-28-rto-program-terms-display.md`.
  The city-page pricing hard stop admits a price only when interpolated from the pricing module,
  ZIP-scoped, with effective date and centroid basis. `$99` is a flat program term from
  `src/data/rtoTerms.ts` and meets none of those. Without the record, a future session reads the
  CTA as a violation and reverts it. Same pattern as the 2026-08-17 override.
- An `id` anchor on `/rent-to-own/`. The page has none today, so "link to that section" needs one.

## Also shipped earlier in this session (2026-08-28), all live-verified

`9a78f7a` centred `/ai-info/`, `/terms/`, `/privacy/`, which were pinned to the left edge by
`article { max-width: 800px }` with no centring or gutter. `fdec056` fixed sitemap lastmod dying on
Cloudflare's shallow clone (8 of 54 in production, now 54 of 54). `dad9939` published both white
parables. `12411c2` expanded `/delivery/` from 347 to 2,636 words and removed two unsourced
statistics. Full record in `sessions/2026-08-28-session.md`.

## Owner's desk

New:
1. **Approve or change the $99 spec.** Blocks everything on T-183.

Standing, unchanged:
2. Live callback E2E test with Doug. Closes T-171.
3. Yard photo shoot. Gates T-170.
4. Three textless Gemini clearance illustrations.
5. VRTO live date from Ryan. Gates T-159.
6. Maps rank recheck ~2026-09-04 vs the #23 baseline. T-155.
7. Request indexing on `/delivery/` and `/container-buying-guide/` (T-175). Note `/delivery/` is no
   longer thin, so this is now worth retrying.
8. The social planning dashboard, whenever he returns to it. n8n is ruled out; it is his work
   account.

## Next session starts here

Read `UDO Project/HANDOFF.md`, then this log, then the spec awaiting approval.

---

## SESSION CLOSED 2026-08-31 on owner request ("get ready for shutdown")

**FINAL STATE: `main == origin == f787819`. Nothing unpushed. 64 built routes, 473 tests / 24 files
green. Build exit 0.**

The session ran five calendar days, 2026-08-27 through 2026-08-31, and shipped **14 commits**, every
one live-verified. Checkpoint: `.checkpoints/2026-08-31-1400-rto-99-shipped/checkpoint.md`.
`HANDOFF.md` carries a fresh 2026-08-31 current block, which is the authoritative entry point.

### The day's arc

T-183, the $99 rent-to-own program change, went from Doug's forwarded text to live in one day:
brainstormed under the skill, spec written and owner-approved, plan written, executed subagent-driven
in three waves, verified live. Ten commits. T-185 opened and closed on an owner ruling the same day.

### What is left, and who owns it

**Owner owns the entire critical path.** Callback E2E test with Doug, yard photos, three Gemini
clearance illustrations, Ryan's VRTO date, the ~2026-09-04 Maps recheck, and re-requesting indexing
on `/delivery/` now that it is 2,636 words rather than 347.

**Not blocked on him:** T-174 (watch five commercial pages for cannibalisation on the next GSC
export), T-176 (blog category pages, sitemap and noindex, revisit condition arguably met at 8
posts), and the T-168 remainder, where **certification post 2 is drafted and verifier-passed but was
never integrated**: `.outputs/content/2026-08-27-bic-provenance-self-check-post.md`. Strip its marked
working-note section before moving it into `src/content/blog/`.

### Two traps this session paid for, recorded so the next one does not

1. **`grep --include=*.html` silently fails under this zsh** and returns a false pass. A
   verification sweep written that way reported "0 pages" while checking nothing, and a follow-up
   loop then falsely flagged a good page. Neither was reported as a finding. Write verification in
   Python.
2. **`route-lastmod-freshness-guard` compares against git committer dates**, so it can only fail
   after a commit exists. When a commit touches a page module, run `npm run generate:route-lastmod`
   and amend the regenerated table into that same commit.

### Honest note on the session's own errors

Three things would have shipped wrong and were caught: two were defects in the orchestrator's own
implementation plan (literal dash bytes in a test; a local-time/UTC mismatch that would have claimed
the program was live a day early), and one was a blog title leaking the figure onto card surfaces
that cannot carry its condition. All three were caught by subagents, not by the orchestrator. The
orchestrator also pushed a layout fix earlier in the session without regenerating the lastmod table,
leaving the deployed sitemap advertising stale dates for three pages until a guard caught it.
