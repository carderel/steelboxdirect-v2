# Session: 2026-08-16

Tags: #resume #owner-decisions #a11y #rental-providers #git-hygiene
LLM: Claude Opus 5, 1M context (claude-opus-5[1m]), Claude Code CLI
Status: OPEN at time of writing. Three agents dispatched, one returned.

Opened on a bare `resume`. The 2026-08-12 through 08-14 session closed with six items parked for the owner
and nothing else in flight, so the whole job on resume was to reconstruct state, put the parked items in
front of him, and execute whatever he authorized.

Branch: main. **Live site still at `0b63011`. Three commits from 08-14 remain unpushed, deliberately.**

---

## What the resume reconstruction found

The auto-injected hook context and the `pending-work` memory were both stale, the memory by twelve days,
and would have been actively misleading if trusted. `pending-work.md` still describes 2026-08-04 as the
present, with Columbus as the newest city page and Locations Phase 2 gated. Since then the site has gone to
55 pages, the rental guide and the guides hub shipped, and the test suite went green. **The three session
logs are the only current record.** Reconstruction ran off `2026-08-14-session.md` and `PROJECT_STATE.json`
rather than off memory, which is the correct order and worth keeping.

Verified independently before acting on any of it:

- `git log origin/main..HEAD` returns exactly three commits, matching the log's claim.
- `git status -sb` confirms `ahead 3`, and the 34 deferred UDO migration deletions are still unstaged.

## The four decisions put to the owner, and his answers

Presented as one question set rather than serially, because all four were his to make and none of them
depended on another's answer.

1. **Push the three commits.** Answer: **push AFTER the contrast fix**, so one deploy carries everything
   rather than two deploys carrying half each.
2. **KOI Rental.** Answer: **confirmed trading, publish.** The 08-14 session refused to infer this from
   "Koi is fine" and asked instead. That was right: the two readings pointed at opposite field values, and
   the field publishes a named third party as verified.
3. **The five remaining 2.77:1 contrast failures.** Answer: **fix only `FaqSection.astro`.** The other four
   are an accepted failure, now recorded as T-136 so a later session does not rediscover them as news.
4. **T-107, the gitignored audit trail.** Answer: **track it.**

## The T-107 mechanism, corrected before authorizing

The prior record says `UDO Project/` is "gitignored wholesale". That is not the mechanism, and the
difference matters for the fix. Checked with `git check-ignore -v` on representative paths:

- `.gitignore:37` is `.checkpoints/` and `:41` is `.project-catalog/`. **Both are unanchored**, so git
  applies them at every directory depth. That, and nothing else, is what swallows the audit trail.
- `UDO Project/PROJECT_STATE.json` and `UDO Project/LESSONS_LEARNED.md` return **NOT IGNORED**. They were
  never ignored, only untracked.
- Line 35's `UDO/` does not match `UDO Project/` at all and is a red herring.

So the fix is to anchor two patterns to `/`, not to carve out exceptions. The UDO-runtime block at lines 84
to 95 stays untouched, and `UDO Project/.project-catalog/backups/` has to be re-verified as still ignored
afterwards, since it was riding on the broad pattern.

**Secret pre-scan before authorizing the commit:** across all 389 files the only matches were
`RESEND_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY` as variable **names** with no values attached. No JWTs, no
`sk-` keys, no live credentials. The agent was told to re-verify independently and to stop without
committing if it found a real value.

## Agents dispatched

Three `astro-developer` dispatches, all constrained to leave the working tree dirty and push nothing.
Because they run concurrently against one tree, the git agent was additionally restricted to explicit
pathspecs, with `git add -A`, `git add .` and `git commit -a` named and forbidden, so the 34 deferred
deletions and the other agents' edits cannot be swept in.

### 1. FaqSection contrast, RETURNED AND CLEAN

`src/components/home/FaqSection.astro`, colour tokens only, copy byte identical. Build exit 0 at 55 pages,
suite 224 passing 0 failing, zero dashes introduced, `git status --porcelain -- src/` shows the one file.

Ten text elements move from failing to passing, the worst of them from 2.36:1 to 5.22:1 and the body block
from 2.77:1 to 6.14:1. The list borders also clear 1.4.11 at 6.14:1.

**Two things the agent got right that a literal copy of the model commit would have got wrong:**

- 5a7422e wrote `.loc-faq .big em{font-style:italic}` with no colour, which is safe there because that
  file's shared rule carries no colour. **This component's shared rule is
  `.big em{...color:var(--c5-permits)...}`**, so dropping the colour would have cascaded orange onto orange
  and made the word "everyone" invisible. It used `color:inherit`, which is the faithful expression of
  "font-style only" in this file. A blind mirror of the model would have shipped an invisible word.
- **`.sh .tg` still fails after the fix**, at 2.98:1, because `opacity:.55` holds it down independently of
  the colour token. The agent declined to touch it and said why: it is outside the authorized fix, and the
  identical opacity ships on `/locations/` and `/shipping-container-guides/`, so fixing it here alone would
  re-create exactly the inconsistency this change was authorized to remove. Recorded in T-136 as a
  **site-wide** decision. It is the only item there still failing on a page the owner now considers fixed.

### 2. KOI publish and count reconciliation, IN FLIGHT

The `status` flip is the small half. Cincinnati moves from two conex operators to three, derived counts
update themselves and hand-written prose does not, and `0b63011` already proved this page can ship two
statements of the same count that disagree. Agent instructed to verify in
`dist/container-rental-guide/index.html` rather than in source, and to report rather than silently rewrite
any copy implying KOI is an Ohio company, since its yards are Florence KY and Dixie Highway.

### 3. T-107 gitignore anchoring and the `UDO Project/` commit, IN FLIGHT

## Open at time of writing

1. **Two agents still running.**
2. **Nothing pushed.** Six commits will go out together once the verifier passes.
3. **T-036**, wiring the now-green HS003 guard to the build. Worth flagging as a decision rather than a
   chore: the guard has a documented false negative (T-114, it cannot see a flat unhedged assertion) and a
   documented false positive risk (T-117), so it will occasionally be wrong in both directions.
4. **T-112**, the 634 pre-existing dashes, deferred by owner ruling.
5. **T-135**, the guard's own header comment, still wrong.
6. **T-136**, the four accepted contrast failures plus the `.sh .tg` opacity question.

## Next

Independent verifier per L002 across the whole combined change set, then one `git push origin main`.
