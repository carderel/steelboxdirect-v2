# Session log: 2026-09-15

Continuation of the session that opened 2026-09-11; the wall clock rolled over again. The narrative
lives in `.project-catalog/sessions/2026-09-11-session.md` and `2026-09-14-session.md`.

## Shipped live in this session
- **T-214**, commits `4476c54` + `5016e97`: 11 state pages at `/locations/{state}/`, areaProfile and
  geography rendered into city HTML, the state breadcrumb, the hub state tier. IndexNow 161 URLs.
- **3D container viewer**, commits `432b325` + `0b353cb`: interactive viewer on all three product
  pages, Three.js vendored first-party, 682 KB of wasted desktop download removed.

## Committed, NOT pushed
- **Warranty claim fix**, commits `b292b8c` + `80e1a9b`. The Lifetime Leak Warranty now states its
  remedy (a self-applied fiberglass patch kit) on all ~19 selling surfaces, including both schema
  modules. 708 tests. `main` is 2 ahead of `origin/main`.
  **Doug should see the wording before it goes live**, since he honors the warranty.

## Design work, not built into the site
- **Product page teardown, artifact v5:** https://claude.ai/code/artifact/ab255674-bc7f-422c-9b3d-2249eaafc90b
  Mock A is now the working proposal: three acts (what it is / how you get it / where and who from),
  dimensions promoted into an aligned block, 3D viewer kept in act one, and a new FAQ with every
  answer traced to a file and line. Mocks B and C are unchanged from v1.

## Open decisions
1. **Push the warranty fix?** Two commits waiting.
2. **Build Mock A into the real template?** It is still only a mockup.
3. **Does the inspection section come back**, with the rejection cost stated alongside it
   (`terms.astro:106`: delivery fee + return delivery fee + 10% cancellation)?
4. **The homepage hero image** still carries the invalid container number "TCKU 123456 7". The
   correct ISO 6346 check digit is 0. Owner said leave it for now.
5. **Real per-size product photos.** Blocked on Doug shooting them; the 2026-08-25 shot list covers it.
6. Carried from earlier: T-208 Supabase A or B, send smallbarndo, the 10ft SKU question.

## Known debt, recorded not chased
- `prod-clearance` is still hidden on the LIVE site, since 2026-08-26, waiting on artwork. It answers
  placement, the most praised moment in 379 reviews. Cheapest available win.
- Three.js ships whole (670 KB raw) because the embeds have no bundler; tree-shaking would cut it.
- `sandbox="allow-scripts"` on the 3D iframe needs an `Access-Control-Allow-Origin` rule on `/3d/*`
  from `renderHeadersFile()`. Low value now that the code is vendored. Optional.
- ~180px of dead width right of the buy box; fixing it means re-proportioning the hero grid.

## Environment
The machine has been thrashing on and off all session. Four background servers were killed for low
memory. The mockup preview server is no longer needed, since the mockups are published in the
artifact.

## Change log, 2026-09-15
- **Owner removed the "who you are buying from" block** from the hero aside, including the "what you
  can check before you pay anyone" link. Flagged once and built as asked. Consequence on the record:
  the page now has NO above-the-fold answer to the corpus's number one fear (will I actually receive
  it), because the three-act order already moved "is this a real company" to the fifth screen.
- Mockup server restarted on port 4455 after memory recovered to 2.4 GB free.
- **Mock A v7 (artifact v7).** Hero aside relabelled `DELIVERY | Timing and truck access`, replacing
  the audit-language header that was a critique of the old page. Spacing reverted from forced to
  content-led: body back to the page's own 15.5px prose size, padding 50px back to 19px, dimensions
  table padding restored. Final column gap 155px, deliberate, about 20% of the column.
- **Nothing on the page links to `#real-company` any more.** It is reachable only by scrolling. No
  replacement link invented; that is the owner's call.
- Saved `whitespace-tolerance.md` to memory: a modest gap is fine, fix a big one by moving real
  content into it, never by stretching type or writing filler.
